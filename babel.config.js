// Keep Babel aligned with the React Native preset so Metro and Jest transform
// the same syntax without extra local overrides.
module.exports = {
  presets: ['module:@react-native/babel-preset'],
};
